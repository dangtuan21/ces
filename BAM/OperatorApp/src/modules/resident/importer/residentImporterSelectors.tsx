import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const residentImporterSelectors = importerSelectors(
  'resident.importer',
);

export default residentImporterSelectors;
