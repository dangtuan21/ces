import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const feedbackImporterSelectors = importerSelectors(
  'feedback.importer',
);

export default feedbackImporterSelectors;
