import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const announcementImporterSelectors = importerSelectors(
  'announcement.importer',
);

export default announcementImporterSelectors;
